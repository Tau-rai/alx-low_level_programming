#include <stdio.h>

/**
 * main - entry point
 * @argc: reps number of arguments
 * @argv: pinter to an array of strings
 * Return: Always 0.
 */
int main(__attribute__((unused))int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (0);
}
