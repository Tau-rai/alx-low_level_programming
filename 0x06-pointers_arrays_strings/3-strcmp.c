#include "main.h"
#include <string.h>

/**
 * _strcmp - compares two strings
 * @s1: first pointer to string
 * @s2: second point to string
 * Return: Always 0.
 */
int _strcmp(char *s1, char *s2)
{
	return (strcpm(s1, s2));
}
