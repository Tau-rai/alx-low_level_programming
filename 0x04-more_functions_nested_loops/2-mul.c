#include "main.h"
#include <ctype.h>

/**
 * mul - multiplies two integers and returns the result
 * @a: first integer to be multiplied by the second
 * @b: second integer
 *
 * Return: 1 Success.
 */

int mul(int a, int b)
{
	int result;

	result = a * b;

	return (result);
}
