#include "main.h"

/**
 * main - checks the code
 *
 * Return: Always o.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. I fear the lack of them - Isaac Asmov";
	_puts(str);
	return (0);
}
