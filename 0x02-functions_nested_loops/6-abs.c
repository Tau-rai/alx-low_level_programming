#include "main.h"

/**
 * _abs - computes the absolute integer of a number
 *@int: represent the integer to be computed into an absolute integer
 * Return: Always 0.
 */

int _abs(int)
{
	/*initialize an integer num*/
	int num;

	if (num < 0)
	{
		num = -num;
	}
	return (0);
}


