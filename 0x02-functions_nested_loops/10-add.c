#include "main.h"

/**
 * add - adds two ints together and returns the result
 * @a: int to be added to b
 * @b: int to be added to a
 *
 * Return: Always 0
 */

int add(int a, int b)
{
	int result;

	result = a + b;

	return (result);
}
