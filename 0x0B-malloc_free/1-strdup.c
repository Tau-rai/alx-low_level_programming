#include "main.h"
#include <stdlib.h>
#include <string.h>

/**
 * _strdup - duplicates a string
 * @str: pointer to a string
 * Return: Always 0.
 */
char *_strdup(char *str)
{
	return (strdup(str));
}
